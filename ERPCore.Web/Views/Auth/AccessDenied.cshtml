@{
    ViewData["Title"] = "Acesso Negado";
    Layout = "_Layout";
}

<div class="row justify-content-center">
    <div class="col-md-6 text-center">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h3><i class="fas fa-exclamation-triangle"></i> Acesso Negado</h3>
            </div>
            <div class="card-body">
                <i class="fas fa-ban text-danger" style="font-size: 4rem;"></i>
                <h4 class="mt-3">Você não tem permissão para acessar esta página.</h4>
                <p class="text-muted">Entre em contato com o administrador do sistema.</p>

                <div class="mt-4">
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary me-2">
                        <i class="fas fa-home"></i> Página Inicial
                    </a>
                    <a asp-controller="Auth" asp-action="Logout" class="btn btn-outline-secondary">
                        <i class="fas fa-sign-out-alt"></i> Fazer Logout
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>